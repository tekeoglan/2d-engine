# Assets

Development assets will remain as ordinary files and will be named through a
versioned JSON manifest beginning in roadmap milestone 6.

Do not add generated packed archives to this directory. Release packaging will
produce them under an ignored build directory.
