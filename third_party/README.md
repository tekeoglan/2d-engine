# Third-Party Dependencies

No dependencies are vendored yet. Milestone 0 will pin each dependency and
record its source, exact version, purpose, and redistribution license here.

Planned categories are SDL3, OpenGL declarations/function loading, PNG image
decoding, TrueType font rasterization, and WAV/OGG audio decoding.
